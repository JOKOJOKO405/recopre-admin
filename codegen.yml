overwrite: true
schema: "./graphql/scheme/**/*.graphql"
documents: "src/**/*.graphql"
generates:
  ./types/graphql/scheme.d.ts:
    plugins:
      - "typescript"
  config:
    declarationKind: interface
    enumsAsTypes: true
    nonOptionalTypename: true
