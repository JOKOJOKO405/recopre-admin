overwrite: true
schema: "https://record-preparation.hasura.app/v1/graphql"
documents: "src/**/*.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
  ./graphql.schema.json:
    plugins:
      - "introspection"
